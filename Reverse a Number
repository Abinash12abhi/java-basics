import java.util.Scanner;
public class Reverse
{
    public static void main(String[] args)
    {
        Scanner sc=new Scanner(System.in);
        int n,sum=0,digit,rev;
        n=sc.nextInt();
        {
            while(n>0)
            {
                digit=(n%10);
                sum=sum*10+digit;
                n=n/10;
            }
        }
        System.out.println(sum);
    }
}
